# Cadastro_via_interface
Cadastro em Banco de dados via Interface Gráfica.

INSTALAR BIBLIOTECA PYINSTALLER (pip install pyinstaller)

Linha de comando para copilar em .EXE (pyinstaller --onefile -w "nome_arquivo.py"
